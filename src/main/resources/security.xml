<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
  xmlns:beans="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security-3.2.xsd">
                        
 
    <http auto-config="true" use-expressions="true">
    	
        <intercept-url pattern="/login" access="hasAnyRole('ROLE_ADMIN','ROLE_TESTER','ROLE_GUEST')" />
        <intercept-url pattern="/*" access="hasAnyRole('ROLE_ADMIN', 'ROLE_TESTER')" />
        <intercept-url pattern="/admin/*" access="hasRole('ROLE_ADMIN')" />
        
        <form-login login-page="/login" 
        			authentication-failure-url="/login" />
        <anonymous username="guest" granted-authority="ROLE_GUEST" />
    </http>
    
 	<jdbc-user-service id="userService"
 		data-source-ref="dataSource" 
 		users-by-username-query=
 			"SELECT USERNAME, PASSWORD, TRUE FROM USER WHERE USERNAME=?"
 		authorities-by-username-query=
 			"SELECT USERNAME, ROLE FROM USER WHERE USERNAME=?" />
 			
 	<user-service id="xmlUserService">
 		<user name="hubbabubba" password="purukumi" authorities="ROLE_ADMIN" />
 	</user-service>
 		

	<authentication-manager>
		<authentication-provider user-service-ref="userService" />
		<authentication-provider user-service-ref="xmlUserService" />
	</authentication-manager>
	
</beans:beans>